# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Models and partitioned binary weights (>100MB)
models/**/*.bin
*.bin
*.gguf

# M1 local checkpoints and large raw evidence
/models/m1/
/artifacts/m1/raw/

# All local model weights and repacked binaries (packed file + manifest stay local)
models/

# Local tool state (Claude Code + Serena) — regenerated per machine, not project source
.claude/
.serena/

# Probe/benchmark stderr scratch — the committed evidence is the .json/.txt, not these
*.err

# Prior-session local helper, superseded by the committed download commands; kept local, not canonical
/scripts/download_m1_models.ps1

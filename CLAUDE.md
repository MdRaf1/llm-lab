# llm-lab

## Agent skills

### Issue tracker

Issues and specs live as GitHub issues on `MdRaf1/llm-lab` (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles, label strings unchanged from the defaults. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
